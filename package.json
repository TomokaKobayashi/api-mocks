{
  "name": "api-mock-server",
  "version": "0.1.15",
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "start": "yarn workspace @chandla/api-mock-server run start",
    "build": "yarn workspaces run build",
    "publish": "yarn workspace @chandla/api-mock-server run publish"
  }
}