{
  "name": "api-mock-server",
  "version": "0.1.16",
  "private": true,
  "workspaces": [
    "common",
    "packages/*",
    "test"
  ],
  "scripts": {
    "start": "yarn workspace test run start",
    "build": "yarn workspaces run build"
  }
}
