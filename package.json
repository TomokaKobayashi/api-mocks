{
  "name": "api-mock-server",
  "version": "0.1.16",
  "private": true,
  "workspaces": [
    "packages/*",
    "test"
  ],
  "scripts": {
    "start": "yarn workspace test run start",
    "build": "yarn workspaces run build"
  }
}